# How to create Windows bootable USb in Ubuntu
# Create a bootable Windows USB (Vista and above) from Ubuntu through WOEUSB software.


`sudo add-apt-repository ppa:nilarimogard/webupd8`

`sudo apt update`

`sudo apt install woeusb`
